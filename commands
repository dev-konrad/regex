# Generate a random folder name
random_folder=$(mktemp -d /tmp/random_folder.XXXXXX)

# Extract the JAR file into the random folder
jar_file="example.jar"
jar xf "$jar_file" -C "$random_folder"

# Find an example file within the extracted JAR
example_file=$(find "$random_folder" -type f | head -n 1)

# Print the example file
cat "$example_file"

# Delete the random folder
rm -rf "$random_folder"
